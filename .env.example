# Supabase Configuration
# Replace these with your actual Supabase URL and anon key
SUPABASE_URL=https://your-supabase-project-url.supabase.co
SUPABASE_ANON_KEY=your-anon-key-goes-here

# Game Configuration
# Optional: Set to 'development' for more verbose logging
NODE_ENV=production

# Notes:
# 1. Create a file named '.env' with your actual values
# 2. The .env file should NOT be committed to version control
# 3. For local development, these variables will be used
# 4. For production, set these in your hosting environment (Netlify, Vercel, etc.) 